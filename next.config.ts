import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["i.ibb.co"], // Add the hostname for your external images here
  },
  /* other config options here */
};

export default nextConfig;
